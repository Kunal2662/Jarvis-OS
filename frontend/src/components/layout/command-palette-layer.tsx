/**
 * Reserved DesktopShell region for the future Command Palette overlay
 * (Ctrl+Shift+P, per MASTER_ROADMAP.md M8 Phase 3). Header's existing
 * "Search Ctrl+K" button (Phase 1) has no handler yet -- wiring it to a
 * real palette is that future task group's job, not this layout
 * region's. Renders nothing today.
 */
export function CommandPaletteLayer() {
  return null;
}
