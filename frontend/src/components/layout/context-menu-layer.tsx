/**
 * Reserved DesktopShell region for the future reusable context-menu
 * system (right-click menus on Sidebar/Dock/Workspace items). Renders
 * nothing today -- no surface registers a context menu yet.
 */
export function ContextMenuLayer() {
  return null;
}
