"""Command-line delivery surfaces.

A sibling of ``infrastructure/api/`` -- both are thin shims over the
same core runtime, and neither owns business logic. See
``mcp_cli.py`` for the ``jarvis mcp ...`` developer tool (Milestone
10.5 Task Group E).
"""
