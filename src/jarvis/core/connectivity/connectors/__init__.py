"""Concrete `IDeviceConnector` implementations -- Milestone 12 Task
Group B, Phase 2 onward.

Phase 1 shipped the port and an empty registry; each later phase adds
one connector module here plus its factory entry in `factory.py`,
without `core/connectivity/registry.py` or `services/connectivity_
service.py` changing. `home_assistant.py` is Phase 2's connector;
`mqtt.py` (Phase 3) is not built yet.
"""
